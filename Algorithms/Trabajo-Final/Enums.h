#pragma once

enum MomentPlay {
	Afternoon,
	Night
};

enum Direction {
	Up,
	Down,
	Left,
	Right
};

enum ActionFarmer {
	PlayFlashLight,
	KeyForSale,
	KeyForBuy,
	Nothing
};


enum Difficult {
	Normal,
	Hard
};
